# CypherPilot — Docker Compose
# ========================
#
# Production-adjacent configuration for local development.
# All services use the same PostgreSQL 16 backend.
#
# Components:
#   postgres — PostgreSQL 16 with health check and named volume
#   backend  — FastAPI application (uvicorn)
#   frontend — Nginx serving the React SPA
#
# Usage:
#   docker compose up --build            # Start everything
#   docker compose up backend -d         # Run in background
#   docker compose down -v               # Stop and remove volumes
#   docker compose logs -f               # Follow logs

services:
  # ── PostgreSQL ──────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: cypherpilot-postgres
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cypherpilot -d cypherpilot"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    environment:
      POSTGRES_USER: cypherpilot
      POSTGRES_PASSWORD: cypherpilot
      POSTGRES_DB: cypherpilot
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - backend

  # ── Backend API ─────────────────────────────────────────────
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cypherpilot-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "8000:8000"
    environment:
      # Database — points to the compose-managed PostgreSQL instance
      DATABASE_URL: "postgresql+asyncpg://cypherpilot:cypherpilot@postgres:5432/cypherpilot"
      DATABASE_SYNC_URL: "postgresql://cypherpilot:cypherpilot@postgres:5432/cypherpilot"

      # Logging
      LOG_LEVEL: "INFO"

      # Application
      APP_NAME: "CypherPilot"
      CORS_ORIGINS: "http://localhost:3000,http://localhost:8000"
    networks:
      - backend

  # ── Frontend SPA ────────────────────────────────────────────
  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile
    container_name: cypherpilot-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "3000:80"
    networks:
      - backend

volumes:
  postgres_data:
    name: cypherpilot-postgres-data

networks:
  backend:
    name: cypherpilot-backend
    driver: bridge
